# ==================================================
# worldstore — Environment Configuration Example
# ==================================================

# --------------------------------------------------
# Server
# --------------------------------------------------

# Port for the local HTTP server
WORLDSTORE_PORT=43721

# Bind address (usually localhost only)
WORLDSTORE_HOST=127.0.0.1


# --------------------------------------------------
# Storage backend
# --------------------------------------------------

# Storage driver to use
# Supported values:
# - local   (filesystem)
# - object  (S3 / GCS compatible)
# - rdb     (relational database)
WORLDSTORE_STORAGE=local


# --------------------------------------------------
# Local filesystem storage (development)
# --------------------------------------------------

# Directory where World files are stored
WORLDSTORE_LOCAL_PATH=./data/worlds


# --------------------------------------------------
# Object storage (S3 / GCS)
# --------------------------------------------------

# Bucket name
WORLDSTORE_OBJECT_BUCKET=

# Optional prefix inside the bucket
WORLDSTORE_OBJECT_PREFIX=worlds/

# Provider hint (optional)
# e.g. s3, gcs
WORLDSTORE_OBJECT_PROVIDER=

# Credentials (provider-specific)
WORLDSTORE_OBJECT_ACCESS_KEY=
WORLDSTORE_OBJECT_SECRET_KEY=


# --------------------------------------------------
# Relational database (TEXT-based storage)
# --------------------------------------------------

# Database connection string
WORLDSTORE_DATABASE_URL=


# --------------------------------------------------
# Authentication
# --------------------------------------------------

# Static bearer token for API access
# (single-user assumption)
WORLDSTORE_AUTH_TOKEN=change-me


# --------------------------------------------------
# World behavior
# --------------------------------------------------

# Enable automatic history on update
WORLDSTORE_ENABLE_HISTORY=true

# Maximum number of history entries to keep
# (0 = unlimited)
WORLDSTORE_HISTORY_LIMIT=0


# --------------------------------------------------
# Runtime
# --------------------------------------------------

# Environment name (development / production)
NODE_ENV=development