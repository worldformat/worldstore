# ==================================================
# worldstore — Environment Configuration Example
# ==================================================

# --------------------------------------------------
# World storage
# --------------------------------------------------

# World storage location
#
# Supported forms:
#
# Local filesystem:
#   ./data/worlds
#
# Google Cloud Storage:
#   gs://my-bucket/path/to/worlds/
#
# (Other object storage providers may be supported later)
#
WORLDSTORE_URL=./data/worlds

# Cache TTL for parsed World responses (milliseconds)
# (Object storage backends only; ignored for local filesystem)
#
# Default: cache disabled
#WORLDSTORE_CACHE_TTL=5000

# --------------------------------------------------
# Cloud Run / GCP deployment
# --------------------------------------------------

# GCP project ID
PROJECT=

# Deployment region (e.g. us-central1)
REGION=

# Cloud Run service name
SERVICE_NAME=worldstore


# --------------------------------------------------
# Container image
# --------------------------------------------------

# Artifact Registry image reference
# Example:
#   us-central1-docker.pkg.dev/my-project/my-repo/worldstore
#
DOCKER_IMAGE=${REGION}-docker.pkg.dev/${PROJECT}/<repo>/<image>


# --------------------------------------------------
# gcloud CLI helpers
# --------------------------------------------------

# Default project for gcloud
CLOUDSDK_CORE_PROJECT=${PROJECT}